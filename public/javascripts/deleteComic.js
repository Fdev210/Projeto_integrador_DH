function onClickSubmit(event) {
    // const id = document.querySelector('inputTitulo').id

}


window.onload = () => {
    const form = document.getElementById("formulario")
    form.addEventListener("submit", onClickSubmit)
}